{
  "name": "ad-runner-component",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.0.0",
    "react": "^18.0.0"
  },
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --format=esm --outfile=dist/index.js --loader:.jsx=jsx"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
